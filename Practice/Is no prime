import java.util.*;
public class Main {
    public static void main(String[] args) {
        Scanner scn = new Scanner(System.in);
        
        int t = scn.nextInt();   // total no of inputs 
        
        int lv = 1;      //lv would go from 1 to n 
    
            while(lv <= t) {      // t times loop 
            
            //PRIME NO LOGIC 
            
            int fac = 2;
            int count = 0;
            
            while(fac*fac <= n) {
                if(n%fac == 0) {
                    count++;
                }
                
                fac++;
            }
            
            //Determining prime or not 
            if(count == 0) {
                System.out.println("prime");
            }
            else {
                System.out.println("not prime");
            }
            
            lv++;    //Lv badh kar bahar jaayega
        }                  
        
    }
}
